<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Athlete Confirmation Page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script>
        window.onload = ()=>{
            if(`<%= status%>`){
                Swal.fire(
                    'สำเร็จ!',
                    'ยืนยันการสมัครเรียบร้อย',
                    'success'
                )
            }
        }
    </script>
</head>

<body>
    <div class="container mt-5">
        <h1 class="mb-3">Athlete Confirmation Page</h1>
        <table class="table">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>AKA</th>
                    <th>Weight</th>
                    <th>Height</th>
                    <th>Team</th>
                    <th class="text-center">Confirmation</th>
                </tr>
            </thead>
            
            <tbody>
                <% if (athletesData.length == 0) { %>
                    <tr>
                        <th class="text-center" scope="row" colspan="6">ไม่มีข้อมูลนักกีฬา</th>
                    </tr>
        
                <% }else { 
                    athletesData.forEach(athlete => { %>
        
                        <tr>
                            <td>
                                <%= athlete.user.fname %>
                                <%= athlete.user.lname %>
                            </td>
                            <td>
                                <%= athlete.nickname %>
                            </td>
                            <td>
                                <%= athlete.weight %>
                            </td>
                            <td>
                                <%= athlete.height %>
                            </td>
                            <td>
                                <%
                                if(athlete.team){%>
                                    <%= athlete.team.teamname %>
                                <%}else{%>
                                    no team
                                <%}%>

                                
                            </td>
                            
                            <td style="width: 205px;">
                                <a class="btn btn-warning px-4 me-4 d-inline-block" href="/athletes/info/<%=athlete._id%>"class="mr-2">info</a>
                                <form method="POST" class="d-inline-block" action="/athletes/info/confirmAthlete/<%= athlete._id %>">
                                    <button class="btn btn-success" type="submit">Confirm</button>
                                </form>
                            </td>
                        </tr>
                    <% }); 
                } %>
            </tbody>
        </table>
    </div>
</body>

</html>
